name: Publish Package to npmjs
on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: publish-with-yarn
        uses: simenandre/publish-with-yarn@v2.0.0
            
